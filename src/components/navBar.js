import React from "react";
import styles from "./cssModules/navbar.module.css";

const Navbar = () => (
  <nav className={styles.navbar}>
    <h1 className={styles.title}>EnriqueTotal</h1>
  </nav>
);

export default Navbar;
